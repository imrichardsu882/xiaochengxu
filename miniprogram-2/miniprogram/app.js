// app.js
App({
  onLaunch: function () {
    this.globalData = {};

    if (!wx.cloud) {
      console.error('请使用 2.2.3 或以上的基础库以使用云能力');
      return;
    }
    
    // =================================================================
    // 【最终修正】
    // 使用您提供的正确云环境ID，确保小程序连接到您自己的数据库。
    // =================================================================
    wx.cloud.init({
      // 您的云开发环境ID
      env: 'cloud1-2gysfwk2b569a3e5', 
      traceUser: true, 
    });
  },
});